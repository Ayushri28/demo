package project1;

import java.util.Scanner;

public class USN {

	
	
	public static void main(String args[])
	{
		Scanner input=new Scanner(System.in);
		String str=new String();
		System.out.println("enter string");
		str=input.next();
		boolean b= Pattern.matches("[12]+[A-Z]+[A-Z]+[0-9]+[0-9]+[[C][S]||[E][I]]+[0-9]+[0-9]+[0-9]+", s );
		while(str.length()==10)
		{
		for(int i=0 ;i<s.length(); i++)
		{
			if((getChar(i)==isDigit()) && (getChar(i)=='1' || getChar(i)=='2'))
				i++;
			else if()
		}
		}
	}
}
